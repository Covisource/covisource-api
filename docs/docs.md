# Covisource API docs

#### GET /

Returns a welcome greeting

#### GET /secretRoute

If authenticated returns authenticated

#### POST /newUser

A route to make new users.

PARAMETERS

```
    name: string
    email: string
    provider: string
```
